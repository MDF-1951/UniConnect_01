# Active Profile - Set via environment variable SPRING_PROFILES_ACTIVE
# For development, no profile is set (uses these default values)
# For production, set SPRING_PROFILES_ACTIVE=prod in Render

# Development Configuration (overridden by application-prod.properties when SPRING_PROFILES_ACTIVE=prod)
spring.application.name=unisocial-backend

# MySQL datasource (Development)
spring.datasource.url=jdbc:mysql://localhost:3306/unisocial?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=test1234

# JPA Configuration (Development)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT configuration (Development)
security.jwt.secret=5f8a3b7d9c2e4f6a8b1d0e2f3c4a6b8d
security.jwt.expiration-ms=86400000

# Server (Development)
server.port=8080

# File Upload Configuration (Development)
file.upload-dir=C:/Users/Mohammed Fahim/Documents/Tech/UniConnect/uploads
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.enabled=true
