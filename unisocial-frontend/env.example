# UniSocial Frontend Environment Configuration
# Backend API URL (Spring Boot runs on port 8080)
REACT_APP_API_URL=http://localhost:8080

# Frontend will run on port 3000 (React default)
# No need to specify PORT as React uses 3000 by default

